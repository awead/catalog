<% @page_title = t('blacklight.search.show.title', :document_title => document_show_html_title, :application_name => application_name) %>

<%= render "shared/record_controls" %>

<%= content_tag :div, :id => document_dom_id, :class => "container-fluid", :itemscope => true, :itemtype => @document.itemtype do %>
  
  <%= render_property_url %>

  <%= render_document_partial @document, :show %>

  <%= render_z3988_title if @document["marc_ss"] %>

<% end %>
