<% if show_sort_and_per_page? and !blacklight_config.sort_fields.blank? %>
  <%= content_tag :div, :class => "dropdown inline" do %>
    <%= link_to render_sort_dropdown_text, "#", :class => "dropdown-toggle", :role => "button", :data => { :toggle => "dropdown" } %>
    <%= content_tag :ul, render_sort_options, :class => "dropdown-menu" %>
  <% end %>
<% end %>